import 'package:flutter/cupertino.dart';

import '../model/User.dart';

class EditForm extends StatelessWidget {
  
  const EditForm({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
  
  User user;
  // fetch from DB i user.name = ...
    return Text('x');
  }
}
